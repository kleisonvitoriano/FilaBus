{
  "name": "fila-virtual-server",
  "version": "1.0.0",
  "description": "Servidor para a Fila Virtual",
  "main": "server.js",
  "scripts": {
    "build": "netlify deploy --prod"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "serverless-http": "^3.2.0"
  }
}
